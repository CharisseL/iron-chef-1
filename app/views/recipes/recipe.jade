.recipe(data-recipe-id=recipe._id.toString())
  button.delete Remove
  .photo(style='background-image:url(#{recipe.photo});')
  .name= recipe.name
  .ingredients
    ul
      each ingredient in recipe.ingredients
        li= ingredient
  .directions= recipe.directions
